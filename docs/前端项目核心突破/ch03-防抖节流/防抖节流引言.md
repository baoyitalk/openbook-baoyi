# 防抖节流 —— 第一性原理拆解

> **面试高频指数：⭐⭐⭐⭐⭐**

> **核心奥义一句话**：防抖是「等你说完」，节流是「我有节奏」—— 两者本质都是用**闭包 + 定时器**控制函数执行频率，区别在于「重置计时」还是「忽略触发」。

---

## 🎯 第一性原理：从「问题」出发

### 为什么需要防抖和节流？

用户交互会产生**高频触发**的事件：

```
用户行为          触发频率              问题
─────────────────────────────────────────────────
输入框输入        每按一个键触发一次      频繁请求接口
窗口 resize      每像素变化触发一次      频繁计算布局
滚动 scroll      每像素滚动触发一次      频繁执行回调
按钮点击         用户疯狂点击            重复提交数据
```

**核心问题**：如何控制函数执行频率，避免性能浪费和逻辑错误？

**两种解决思路**：

```
┌─────────────────────────────────────────────────────────────────┐
│                     高频事件触发                                 │
│                          │                                      │
│          ┌───────────────┴───────────────┐                      │
│          ▼                               ▼                      │
│       防抖 Debounce                   节流 Throttle              │
│     "等你说完我再做"                  "我有自己的节奏"             │
│          │                               │                      │
│          ▼                               ▼                      │
│    等待停止触发后执行                 固定间隔执行一次              │
│    (最后一次生效)                    (第一次生效,后续忽略)          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 一、防抖 Debounce

### 1.1 核心概念

> **防抖**：事件触发后，等待 n 秒再执行。如果 n 秒内再次触发，则**重新计时**。

### 📊 生活比喻

```
┌─────────────────────────────────────────────────────────────────┐
│                        电梯关门                                  │
│                                                                 │
│  场景：电梯门准备关闭，有人进来就重新等待                           │
│                                                                 │
│  时间线：                                                        │
│  ─────────────────────────────────────────────────────────────  │
│  │人进来│    │人进来│    │人进来│         │关门│                  │
│  └──────┘    └──────┘    └──────┘  等3秒   └────┘                │
│       ↓           ↓           ↓             ↓                   │
│    重新计时     重新计时    重新计时      最终执行                 │
│                                                                 │
│  结论：只有最后一次触发后等待足够时间，才会执行                     │
└─────────────────────────────────────────────────────────────────┘
```

### 📊 执行时机可视化

```
事件触发: ──●──●──●──●──●────────────────────●──●────────────────
                                           │
                      等待时间(delay)       │      等待时间
              ◀─────────────────▶          │  ◀──────────────▶
                                           │
函数执行: ─────────────────────────────────★─────────────────────★
                                     只执行这一次           只执行这一次

图例: ● = 事件触发  ★ = 函数实际执行
```

### 1.2 实现思路

```
防抖核心逻辑：
┌────────────────────────────────────────┐
│  1. 声明定时器变量（闭包保存）            │
│  2. 返回新函数                          │
│  3. 每次触发：                          │
│     ├─ 清除之前的定时器（重新计时）       │
│     └─ 设置新的定时器                   │
│  4. 定时器到期：执行原函数               │
└────────────────────────────────────────┘
```

### 1.3 基础版实现

```javascript
/**
 * 防抖函数 - 基础版
 * @param {Function} fn - 要执行的函数
 * @param {Number} delay - 延迟时间(ms)
 */
function debounce(fn, delay) {
    let timer = null;  // 闭包保存定时器
    
    return function(...args) {
        // 每次触发都清除之前的定时器
        if (timer) {
            clearTimeout(timer);
        }
        
        // 重新设置定时器
        timer = setTimeout(() => {
            fn.apply(this, args);  // 保持 this 指向和参数
        }, delay);
    };
}

// 使用示例
const searchInput = document.getElementById('search');
searchInput.addEventListener('input', debounce(function(e) {
    console.log('搜索:', e.target.value);
    // 发送 API 请求
}, 500));
```

### 1.4 进阶版实现（立即执行 + 取消功能）

```javascript
/**
 * 防抖函数 - 进阶版
 * @param {Function} fn - 要执行的函数
 * @param {Number} delay - 延迟时间(ms)
 * @param {Boolean} immediate - 是否立即执行第一次
 */
function debounce(fn, delay, immediate = false) {
    let timer = null;
    
    const debounced = function(...args) {
        const context = this;
        
        // 清除定时器
        if (timer) {
            clearTimeout(timer);
        }
        
        if (immediate) {
            // 立即执行模式
            const callNow = !timer;  // 第一次触发时 timer 为 null
            
            timer = setTimeout(() => {
                timer = null;  // delay 后重置，允许下次立即执行
            }, delay);
            
            if (callNow) {
                fn.apply(context, args);
            }
        } else {
            // 延迟执行模式
            timer = setTimeout(() => {
                fn.apply(context, args);
            }, delay);
        }
    };
    
    // 取消功能
    debounced.cancel = function() {
        if (timer) {
            clearTimeout(timer);
            timer = null;
        }
    };
    
    return debounced;
}

// 使用示例
const debouncedFn = debounce(handleSearch, 500, true);
input.addEventListener('input', debouncedFn);

// 需要时可以取消
debouncedFn.cancel();
```

### 📊 立即执行 vs 延迟执行

```
【延迟执行】immediate = false (默认)
事件触发: ──●──●──●──●──●────────────────
                              │
                        等待 delay
                              │
函数执行: ────────────────────────────★
                               最后执行一次

【立即执行】immediate = true
事件触发: ──●──●──●──●──●────────────────
           │
           ▼
函数执行: ──★──────────────────────────────
        第一次立即执行，后续触发被忽略
        等 delay 后重置，下一轮可再次立即执行
```

---

## 二、节流 Throttle

### 2.1 核心概念

> **节流**：每隔 n 秒执行一次函数。在 n 秒内，无论触发多少次，**只执行一次**。

### 📊 生活比喻

```
┌─────────────────────────────────────────────────────────────────┐
│                      水龙头/技能CD                               │
│                                                                 │
│  场景1 - 水龙头：                                                │
│  即使管道里有更多水，水龙头也只能按固定流速出水                     │
│                                                                 │
│  场景2 - 游戏技能：                                              │
│  技能CD中，无论怎么按都不会触发，CD好了才能再次释放                  │
│                                                                 │
│  时间线：                                                        │
│  ─────────────────────────────────────────────────────────────  │
│   │按│按│按│按│    │按│按│按│按│    │按│按│按│按│                │
│    ↓               ↓               ↓                            │
│   执行(开始CD)    执行(CD好了)    执行(CD好了)                    │
│   ◀──CD时间──▶   ◀──CD时间──▶                                   │
└─────────────────────────────────────────────────────────────────┘
```

### 📊 执行时机可视化

```
事件触发: ──●●●●●●●●●●●●●●●●●●●●●●●●●●●●●●●●●●●●●──
           │        │        │        │        │
           ▼        ▼        ▼        ▼        ▼
函数执行: ──★────────★────────★────────★────────★──
           │◀─间隔─▶│◀─间隔─▶│◀─间隔─▶│◀─间隔─▶│
           
无论触发多频繁，每隔固定时间只执行一次
```

### 2.2 实现思路

```
节流核心逻辑：
┌────────────────────────────────────────┐
│  1. 声明定时器变量（闭包保存）            │
│  2. 返回新函数                          │
│  3. 每次触发检查：                       │
│     ├─ 有定时器？→ 直接返回（忽略）       │
│     └─ 没定时器？→ 设置定时器            │
│  4. 定时器回调：执行函数 + 清空定时器     │
└────────────────────────────────────────┘
```

### 2.3 定时器版实现

```javascript
/**
 * 节流函数 - 定时器版
 * 特点：第一次不会立即执行，最后一次会执行
 */
function throttle(fn, interval) {
    let timer = null;
    
    return function(...args) {
        // 如果定时器存在，说明还在冷却中，直接返回
        if (timer) return;
        
        // 设置定时器
        timer = setTimeout(() => {
            fn.apply(this, args);
            timer = null;  // 执行完毕，清空定时器，允许下次执行
        }, interval);
    };
}
```

### 2.4 时间戳版实现

```javascript
/**
 * 节流函数 - 时间戳版
 * 特点：第一次会立即执行，最后一次可能不执行
 */
function throttle(fn, interval) {
    let lastTime = 0;  // 上次执行时间
    
    return function(...args) {
        const now = Date.now();
        
        // 距离上次执行超过间隔时间
        if (now - lastTime >= interval) {
            fn.apply(this, args);
            lastTime = now;  // 更新上次执行时间
        }
    };
}
```

### 2.5 完整版实现（首次 + 末次都执行）

```javascript
/**
 * 节流函数 - 完整版
 * 结合时间戳和定时器，确保首次和末次都执行
 */
function throttle(fn, interval, options = {}) {
    let timer = null;
    let lastTime = 0;
    const { leading = true, trailing = true } = options;
    
    return function(...args) {
        const context = this;
        const now = Date.now();
        
        // 第一次是否执行
        if (!lastTime && leading === false) {
            lastTime = now;
        }
        
        const remaining = interval - (now - lastTime);
        
        if (remaining <= 0 || remaining > interval) {
            // 时间到了，执行
            if (timer) {
                clearTimeout(timer);
                timer = null;
            }
            fn.apply(context, args);
            lastTime = now;
        } else if (!timer && trailing !== false) {
            // 设置定时器，保证最后一次执行
            timer = setTimeout(() => {
                lastTime = leading === false ? 0 : Date.now();
                timer = null;
                fn.apply(context, args);
            }, remaining);
        }
    };
}

// 使用示例
window.addEventListener('scroll', throttle(function() {
    console.log('滚动位置:', window.scrollY);
}, 200));
```

---

## 三、防抖 vs 节流 对比

### 📊 核心区别可视化

```
┌─────────────────────────────────────────────────────────────────┐
│                    防抖 vs 节流 执行对比                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  事件触发: ●●●●●●●●●●────────●●●●●●●●●●────────                  │
│           └──连续触发──┘      └──连续触发──┘                     │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│                                                                 │
│  防抖执行: ──────────────★────────────────────★────             │
│                   等停止后执行         等停止后执行               │
│           "等你说完我再做"                                       │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│                                                                 │
│  节流执行: ★────★────★────★────★────★────★────★────             │
│           │固定│固定│固定│固定│固定│固定│固定│                    │
│           "不管你怎么催，我按自己节奏来"                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 📊 特性对比表

| 特性 | 防抖 Debounce | 节流 Throttle |
|------|---------------|---------------|
| **核心思想** | 等待停止后执行 | 固定间隔执行 |
| **触发时机** | 最后一次触发后 | 间隔时间到达时 |
| **执行次数** | 可能只执行1次 | 按间隔多次执行 |
| **比喻** | 电梯等人关门 | 水龙头匀速出水 |
| **重置机制** | 有新触发就重置定时器 | 不重置，忽略新触发 |

### 📊 适用场景对比

```
┌─────────────────────────────────────────────────────────────────┐
│                        适用场景                                  │
├────────────────────────────┬────────────────────────────────────┤
│         防抖               │              节流                   │
├────────────────────────────┼────────────────────────────────────┤
│ • 搜索框输入               │ • 滚动加载更多                      │
│   (等用户输完再请求)        │   (固定间隔检查位置)                │
│                            │                                    │
│ • 窗口 resize              │ • 鼠标移动事件                      │
│   (等调整完再计算布局)      │   (固定间隔更新位置)                │
│                            │                                    │
│ • 按钮提交                 │ • 射击游戏开枪                      │
│   (防止重复提交)            │   (固定射速)                        │
│                            │                                    │
│ • 表单验证                 │ • 视频播放进度                      │
│   (等输完再验证)            │   (固定间隔上报)                    │
└────────────────────────────┴────────────────────────────────────┘
```

---

## 四、底层原理深度剖析

### 4.1 闭包的作用

```javascript
function debounce(fn, delay) {
    let timer = null;  // ← 这个变量被闭包保存
    
    return function(...args) {
        // 每次调用都能访问同一个 timer
        clearTimeout(timer);
        timer = setTimeout(() => {
            fn.apply(this, args);
        }, delay);
    };
}

// 闭包机制图解：
// ┌─────────────────────────────────────────┐
// │  debounce 函数作用域                     │
// │  ┌─────────────────────────────────┐    │
// │  │  timer = null                   │    │
// │  │                                 │    │
// │  │  ┌───────────────────────────┐  │    │
// │  │  │  返回的函数               │  │    │
// │  │  │  可以访问外层的 timer     │  │    │
// │  │  │  形成闭包                 │  │    │
// │  │  └───────────────────────────┘  │    │
// │  └─────────────────────────────────┘    │
// └─────────────────────────────────────────┘
```

### 4.2 this 和 arguments 的保持

```javascript
// 为什么要用 apply？

const obj = {
    name: 'Claude',
    greet: debounce(function() {
        console.log(this.name);  // 需要正确的 this
    }, 500)
};

obj.greet();  // 如果不用 apply，this 可能指向 window

// 正确做法：
timer = setTimeout(() => {
    fn.apply(this, args);  // 保持调用时的 this 和参数
}, delay);
```

### 4.3 requestAnimationFrame 优化版

```javascript
/**
 * 使用 RAF 的节流 - 更适合动画场景
 * 自动匹配屏幕刷新率（通常60fps，约16.67ms）
 */
function throttleRAF(fn) {
    let ticking = false;
    
    return function(...args) {
        if (ticking) return;
        
        ticking = true;
        requestAnimationFrame(() => {
            fn.apply(this, args);
            ticking = false;
        });
    };
}

// 使用场景：滚动动画、拖拽等
window.addEventListener('scroll', throttleRAF(function() {
    // 更新动画
}));
```

---

## 五、面试高频代码

### 5.1 手写防抖（必会）

```javascript
// 面试最简版 - 30秒写完
function debounce(fn, delay) {
    let timer = null;
    return function(...args) {
        clearTimeout(timer);
        timer = setTimeout(() => fn.apply(this, args), delay);
    };
}
```

### 5.2 手写节流（必会）

```javascript
// 面试最简版 - 定时器版
function throttle(fn, interval) {
    let timer = null;
    return function(...args) {
        if (timer) return;
        timer = setTimeout(() => {
            fn.apply(this, args);
            timer = null;
        }, interval);
    };
}

// 面试最简版 - 时间戳版
function throttle(fn, interval) {
    let last = 0;
    return function(...args) {
        const now = Date.now();
        if (now - last >= interval) {
            fn.apply(this, args);
            last = now;
        }
    };
}
```

### 5.3 实际应用示例

```javascript
// ===== 搜索框防抖 =====
const searchInput = document.getElementById('search');
const debouncedSearch = debounce(async function(e) {
    const keyword = e.target.value;
    if (!keyword) return;
    
    const results = await fetch(`/api/search?q=${keyword}`);
    // 渲染结果...
}, 300);

searchInput.addEventListener('input', debouncedSearch);

// ===== 滚动加载节流 =====
const throttledScroll = throttle(function() {
    const { scrollTop, scrollHeight, clientHeight } = document.documentElement;
    
    // 距离底部100px时加载更多
    if (scrollHeight - scrollTop - clientHeight < 100) {
        loadMore();
    }
}, 200);

window.addEventListener('scroll', throttledScroll);

// ===== 按钮防重复点击 =====
const submitBtn = document.getElementById('submit');
submitBtn.addEventListener('click', debounce(async function() {
    this.disabled = true;
    await submitForm();
    this.disabled = false;
}, 1000, true));  // 立即执行，防止第一次也延迟
```

---

## 六、Lodash 中的实现

```javascript
// Lodash 的 debounce 支持更多选项
import { debounce, throttle } from 'lodash';

// 防抖
const debouncedFn = debounce(fn, 200, {
    leading: false,   // 是否在延迟开始前调用
    trailing: true,   // 是否在延迟结束后调用
    maxWait: 1000     // 最大等待时间
});

// 节流
const throttledFn = throttle(fn, 200, {
    leading: true,    // 是否在节流开始前调用
    trailing: true    // 是否在节流结束后调用
});

// 取消
debouncedFn.cancel();
throttledFn.cancel();

// 立即执行
debouncedFn.flush();
throttledFn.flush();
```

---

## 七、完整知识图谱

```
                        高频事件优化
                             │
            ┌────────────────┴────────────────┐
            │                                 │
         防抖                               节流
       Debounce                           Throttle
            │                                 │
     "等你说完"                          "我有节奏"
            │                                 │
    ┌───────┴───────┐                ┌───────┴───────┐
    │               │                │               │
  延迟执行       立即执行          定时器版       时间戳版
  (默认)      (immediate)        (尾部执行)     (头部执行)
    │               │                │               │
    └───────┬───────┘                └───────┬───────┘
            │                                │
         闭包 + 定时器                    闭包 + 定时器
            │                                │
            └────────────┬───────────────────┘
                         │
                    共同本质：
              控制函数执行频率，优化性能
```

---

## 八、面试速查清单

### ✅ 必记要点

| 知识点 | 防抖 | 节流 |
|--------|------|------|
| **一句话定义** | 等待停止触发后执行 | 固定间隔执行一次 |
| **核心机制** | 重置定时器 | 忽略触发 |
| **实现关键** | clearTimeout + setTimeout | timer存在就return |
| **典型场景** | 搜索输入、resize | 滚动、mousemove |
| **比喻** | 电梯等人 | 水龙头流水 |

### 🎯 面试高频问题

1. **什么是防抖和节流？区别是什么？**
   → 防抖等停止后执行，节流固定间隔执行

2. **手写一个防抖函数**
   → 闭包保存timer，每次清除后重设

3. **手写一个节流函数**
   → 闭包保存timer/lastTime，有则return

4. **说说应用场景**
   → 搜索框用防抖，滚动监听用节流

5. **为什么要用 apply？**
   → 保持 this 指向和参数传递

---

## 🔑 核心奥义

> **记住这个对比，面试稳过：**
>
> | | 防抖 | 节流 |
> |---|---|---|
> | **比喻** | 电梯等最后一人 | 水龙头匀速出水 |
> | **行为** | 重新计时 | 忽略触发 |
> | **结果** | 只执行最后一次 | 固定频率执行 |


>
> **底层都是：闭包 + 定时器 = 控制执行频率**






**核心内容：**
- **第一性原理**：从「高频事件问题」出发，推导出两种解决思路
- **生活比喻**：电梯等人（防抖） vs 水龙头/技能CD（节流）
- **ASCII可视化图解**：执行时机对比、闭包原理、适用场景

**代码实现：**
- 防抖基础版 + 立即执行进阶版
- 节流定时器版 + 时间戳版 + 完整版
- 面试最简版（30秒手写）
- 实际应用示例（搜索框、滚动加载、按钮防重复）

**核心奥义**：防抖是「等你说完」，节流是「我有节奏」—— 底层都是用**闭包 + 定时器**控制函数执行频率。


