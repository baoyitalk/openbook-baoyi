# 函数柯里化 - 第一性原理完全指南

> 🎯 **面试高频指数：★★☆☆☆**

---

## 📚 目录

1. [第一性原理：什么是函数柯里化？](#第一性原理什么是函数柯里化)
2. [函数柯里化的实现](#函数柯里化的实现)
3. [函数柯里化的作用](#函数柯里化的作用)
4. [经典面试题](#经典面试题)
5. [核心奥义](#核心奥义)

---

## 第一性原理：什么是函数柯里化？

### 🧠 定义

**函数柯里化是指将使用多个参数的函数转化成一系列使用一个参数的函数的技术，它返回一个新的函数，这个新函数去处理剩余的参数。**

### 📐 直观理解

```
┌─────────────────────────────────────────────────────────────────┐
│                      函数柯里化的本质                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   【普通函数】一次性接收所有参数                                  │
│                                                                 │
│   function add(a, b) {                                          │
│       return a + b;                                             │
│   }                                                             │
│   add(1, 2)  // 3                                              │
│                                                                 │
│        ┌───┐                                                    │
│        │ a │──┐                                                 │
│        └───┘  │    ┌─────────┐                                  │
│               ├───→│  add    │───→  结果                        │
│        ┌───┐  │    └─────────┘                                  │
│        │ b │──┘                                                 │
│        └───┘                                                    │
│                                                                 │
│   ─────────────────────────────────────────────────────────────  │
│                                                                 │
│   【柯里化函数】分步接收参数                                      │
│                                                                 │
│   const addCurry = curry(add);                                  │
│   addCurry(1)(2)  // 3                                         │
│                                                                 │
│        ┌───┐    ┌─────────┐    ┌───┐    ┌─────────┐            │
│        │ a │───→│ curry   │───→│ b │───→│ 新函数   │───→ 结果   │
│        └───┘    │ 返回新函数│    └───┘    │ 计算结果 │            │
│                 └─────────┘             └─────────┘            │
│                                                                 │
│   参数可以分多次传入，直到参数数量满足原函数要求                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 📝 基本示例

```javascript
// ex:
function add(a, b) {
    return a + b;
}
add(1, 2) // 3

// 柯里化
const addCurry = curry(add);
addCurry(1)(2) // 3
```

### 🔑 柯里化的核心特征

```
╔═══════════════════════════════════════════════════════════════════╗
║                                                                   ║
║   柯里化 = 把 fn(a, b, c) 转换成 fn(a)(b)(c)                       ║
║                                                                   ║
║   本质：将多参数函数转换为一系列单参数函数的链式调用                 ║
║                                                                   ║
╚═══════════════════════════════════════════════════════════════════╝
```

---

## 函数柯里化的实现

### 💡 实现思路

**通过函数的 `length` 属性获取函数的形参个数，形参的个数就是所需参数的个数**

### 📐 实现原理图解

```
┌─────────────────────────────────────────────────────────────────┐
│                    柯里化实现原理                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   function add(a, b, c, d) { return a + b + c + d; }           │
│                                                                 │
│   add.length = 4  ← 需要 4 个参数                                │
│                                                                 │
│   curry(add) 的执行过程：                                        │
│                                                                 │
│   addCurry(1)                                                   │
│       │                                                         │
│       ↓  收集参数 [1]，长度 1 < 4，返回新函数                     │
│   addCurry(1)(2)                                                │
│       │                                                         │
│       ↓  收集参数 [1,2]，长度 2 < 4，返回新函数                   │
│   addCurry(1)(2)(3)                                             │
│       │                                                         │
│       ↓  收集参数 [1,2,3]，长度 3 < 4，返回新函数                 │
│   addCurry(1)(2)(3)(4)                                          │
│       │                                                         │
│       ↓  收集参数 [1,2,3,4]，长度 4 >= 4，执行原函数！            │
│       │                                                         │
│       ↓                                                         │
│      10                                                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 📝 完整实现代码

```javascript
function curry(fn) {
    return _curry.call(this, fn, fn.length);
}

function _curry(fn, len, ...args) {
    return function(...params) {
        // 合并已收集的参数和新传入的参数
        const _args = args.concat(params);
        
        // 判断参数是否足够
        if (_args.length >= len) {
            // 参数足够，执行原函数
            return fn.apply(this, _args);
        } else {
            // 参数不够，继续收集
            return _curry.call(this, fn, len, ..._args);
        }
    }
}

// test
function add(a, b, c, d) {
    return a + b + c + d;
}

const addCurry = curry(add);
console.log(addCurry(1)(2)(3)(4))   // 10
console.log(addCurry(1, 2, 3)(4))   // 10
console.log(addCurry(1)(2, 3, 4))   // 10
console.log(addCurry(1, 2)(3, 4))   // 10
```

### 📐 代码执行流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                 addCurry(1)(2)(3)(4) 执行流程                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   curry(add)                                                    │
│       │                                                         │
│       ↓                                                         │
│   _curry(add, 4, [])  ← len=4, args=[]                         │
│       │                                                         │
│       ↓ 返回函数                                                │
│   ┌──────────────────────────────────────────┐                  │
│   │  function(...params) {                   │                  │
│   │      _args = [].concat(params)           │                  │
│   │      if (_args.length >= 4) ...          │                  │
│   │  }                                       │                  │
│   └──────────────────────────────────────────┘                  │
│       │                                                         │
│       ↓ 调用 (1)                                                │
│   _args = [1], 1 < 4, 继续收集                                  │
│       │                                                         │
│       ↓ 返回新函数，调用 (2)                                     │
│   _args = [1,2], 2 < 4, 继续收集                                │
│       │                                                         │
│       ↓ 返回新函数，调用 (3)                                     │
│   _args = [1,2,3], 3 < 4, 继续收集                              │
│       │                                                         │
│       ↓ 返回新函数，调用 (4)                                     │
│   _args = [1,2,3,4], 4 >= 4, 执行 add(1,2,3,4)                 │
│       │                                                         │
│       ↓                                                         │
│      10                                                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 函数柯里化的作用

### 🎯 三大核心作用

```
┌─────────────────────────────────────────────────────────────────┐
│                    函数柯里化的三大作用                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   1️⃣  参数复用                                                   │
│   ──────────────────────────────────────────────────────────    │
│   本质上来说就是降低通用性，提高适用性                            │
│                                                                 │
│   假如一个函数需要两个参数，其中一个参数可能多次调用并            │
│   不会发生更改，比如商品打折的力度，此时可以根据折扣进行封装      │
│                                                                 │
│   示例：                                                         │
│   ┌────────────────────────────────────────────────────────┐    │
│   │  // 通用的计算折扣函数                                  │    │
│   │  function discount(price, discount) {                  │    │
│   │      return price * discount;                          │    │
│   │  }                                                     │    │
│   │                                                        │    │
│   │  // 柯里化后，固定折扣参数                              │    │
│   │  const discount80 = curry(discount)(0.8);  // 8折      │    │
│   │  const discount50 = curry(discount)(0.5);  // 5折      │    │
│   │                                                        │    │
│   │  discount80(100);  // 80                               │    │
│   │  discount80(200);  // 160                              │    │
│   │  discount50(100);  // 50                               │    │
│   └────────────────────────────────────────────────────────┘    │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   2️⃣  提前返回                                                   │
│   ──────────────────────────────────────────────────────────    │
│   经典实例：元素绑定事件监听器，区分 IE 浏览器的                  │
│   attachEvent 方法                                              │
│                                                                 │
│   示例：                                                         │
│   ┌────────────────────────────────────────────────────────┐    │
│   │  // 只判断一次浏览器类型，之后直接使用                   │    │
│   │  const bindEvent = (function() {                       │    │
│   │      if (window.addEventListener) {                    │    │
│   │          return function(el, type, fn) {               │    │
│   │              el.addEventListener(type, fn);            │    │
│   │          }                                             │    │
│   │      } else {                                          │    │
│   │          return function(el, type, fn) {               │    │
│   │              el.attachEvent('on' + type, fn);          │    │
│   │          }                                             │    │
│   │      }                                                 │    │
│   │  })();                                                 │    │
│   └────────────────────────────────────────────────────────┘    │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   3️⃣  延迟计算                                                   │
│   ──────────────────────────────────────────────────────────    │
│   柯里化函数不会立即执行计算，第一次只是返回一个函数，            │
│   后面的调用才会进行计算                                         │
│                                                                 │
│   示例：                                                         │
│   ┌────────────────────────────────────────────────────────┐    │
│   │  const add = curry((a, b, c) => a + b + c);            │    │
│   │                                                        │    │
│   │  const step1 = add(1);      // 不计算，返回函数        │    │
│   │  const step2 = step1(2);    // 不计算，返回函数        │    │
│   │  const result = step2(3);   // 此时才计算！返回 6      │    │
│   └────────────────────────────────────────────────────────┘    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 经典面试题

### 🔥 面试题：实现 add 方法

**实现一个 add 方法，使计算结果能够满足如下预期：**

```javascript
console.log(add(1)(2)(3)(4, 5)())   // 15
console.log(add(1)(2)(3, 4, 5)())   // 15
console.log(add(1)(2, 3, 4, 5)())   // 15
```

### 📐 解题思路

```
┌─────────────────────────────────────────────────────────────────┐
│                         解题思路                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   这道题的特点：                                                 │
│                                                                 │
│   1. 参数数量不固定（不能用 fn.length 判断）                     │
│   2. 最后通过空调用 () 来触发计算                                │
│   3. 需要持续收集参数，直到空调用时求和                          │
│                                                                 │
│   执行流程：                                                     │
│                                                                 │
│   add(1)  → 收集 [1]，返回函数                                  │
│      ↓                                                          │
│   (2)     → 收集 [1,2]，返回函数                                │
│      ↓                                                          │
│   (3)     → 收集 [1,2,3]，返回函数                              │
│      ↓                                                          │
│   (4,5)   → 收集 [1,2,3,4,5]，返回函数                          │
│      ↓                                                          │
│   ()      → 参数为空！执行求和 → 15                              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 📝 解法一：基于原始求和函数的柯里化

```javascript
function add1(...args) {
    // 求和
    return args.reduce((a, b) => a + b)
}

function currying(fn, ...args) {
    return function (...rest) {
        if (rest.length) {
            // 如果还有参数，继续收集
            return currying(fn, ...args, ...rest)
        } else {
            // 参数为空，执行原函数
            return fn.call(this, ...args)
        }
    }
}

let add = currying(add1)

console.log(add(1)(2)(3)(4, 5)())   // 15
console.log(add(1)(2)(3, 4, 5)())   // 15
console.log(add(1)(2, 3, 4, 5)())   // 15
```

### 📝 解法二：更简洁的写法

```javascript
function add(...args) {
    let allArgs = [...args];
    
    const fn = function (...args2) {
        if (args2.length) {
            // 有参数，继续收集
            allArgs = [...allArgs, ...args2];
            return fn;
        } else {
            // 无参数，计算结果
            return allArgs.reduce((pre, curr) => pre + curr, 0)
        }
    }
    
    return fn;
}

console.log(add(1)(2)(3)())         // 6
console.log(add(1, 2, 3)(4)())      // 10
console.log(add(1)(2)(3)(4)(5)())   // 15
```

### 📐 解法二执行流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                 add(1)(2)(3)(4, 5)() 执行流程                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   add(1)                                                        │
│       │                                                         │
│       ↓  allArgs = [1]                                         │
│       ↓  返回 fn                                                │
│                                                                 │
│   fn(2)                                                         │
│       │                                                         │
│       ↓  args2 = [2]，有参数                                    │
│       ↓  allArgs = [1, 2]                                      │
│       ↓  返回 fn                                                │
│                                                                 │
│   fn(3)                                                         │
│       │                                                         │
│       ↓  args2 = [3]，有参数                                    │
│       ↓  allArgs = [1, 2, 3]                                   │
│       ↓  返回 fn                                                │
│                                                                 │
│   fn(4, 5)                                                      │
│       │                                                         │
│       ↓  args2 = [4, 5]，有参数                                 │
│       ↓  allArgs = [1, 2, 3, 4, 5]                             │
│       ↓  返回 fn                                                │
│                                                                 │
│   fn()                                                          │
│       │                                                         │
│       ↓  args2 = []，无参数！                                   │
│       ↓  执行 reduce 求和                                       │
│       ↓  1 + 2 + 3 + 4 + 5 = 15                                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 两种柯里化的对比

```
┌────────────────────────────────────────────────────────────────────────┐
│                      两种柯里化模式对比                                  │
├────────────────────────────────────────────────────────────────────────┤
│                                                                        │
│   【固定参数柯里化】                   【不定参数柯里化】                 │
│                                                                        │
│   基于 fn.length 判断                 基于空调用 () 触发                │
│                                                                        │
│   curry(add)(1)(2)(3)(4)             add(1)(2)(3)(4)()                 │
│                    ↑                              ↑                    │
│           参数够了自动执行               需要空调用触发                  │
│                                                                        │
│   适用：参数数量固定的函数              适用：参数数量不固定              │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 核心奥义

```
╔═══════════════════════════════════════════════════════════════════════╗
║                                                                       ║
║   🎯 一句话总结：                                                      ║
║                                                                       ║
║   柯里化本质是"化整为零"，把多参函数变成链式单参调用；                   ║
║   核心靠闭包收集参数，length 够了就执行，不够就继续等；                  ║
║   参数复用省重复，提前返回提性能，延迟计算更灵活！                       ║
║                                                                       ║
╚═══════════════════════════════════════════════════════════════════════╝
```

---

## 📋 速记卡片

```
┌─────────────────────────────────────────────────────────────────┐
│                      函数柯里化速记                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  【定义】                                                        │
│   将 fn(a, b, c) 转换为 fn(a)(b)(c) 的技术                      │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  【实现核心】                                                    │
│   1. 通过 fn.length 获取所需参数数量                            │
│   2. 收集参数，判断是否足够                                      │
│   3. 够了执行原函数，不够返回新函数继续收集                       │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  【三大作用】                                                    │
│   ✓ 参数复用：固定部分参数，降低通用性提高适用性                  │
│   ✓ 提前返回：只判断一次，之后直接使用                           │
│   ✓ 延迟计算：参数收集完才计算                                   │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  【面试题关键】                                                   │
│   不定参数柯里化：用空调用 () 触发计算                           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

> 📝 **作者提示**：柯里化虽然面试频率不高，但理解它对于理解闭包和函数式编程很有帮助！