# 数组查找方法 —— 第一性原理拆解

> **面试高频指数：⭐⭐⭐☆☆**

> **核心奥义一句话**：`filter` 是「筛子」返回所有符合条件的元素（数组），`find` 是「探针」只返回第一个符合条件的元素（单值）。记住：**filter 捞一网，find 钓一条**。

---

## 🎯 第一性原理：从「查找需求」出发

### 数组查找的两种本质需求

```
┌─────────────────────────────────────────────────────────────────┐
│                     数组查找的本质需求                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   原始数组: [1, 2, 3, 4, 5, 6]                                   │
│   查找条件: 值等于 2                                             │
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │  需求一：找出「所有」符合条件的元素                        │   │
│   │  → 返回数组 [2]                                          │   │
│   │  → 使用 filter()                                         │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │  需求二：找出「第一个」符合条件的元素                      │   │
│   │  → 返回单值 2                                            │   │
│   │  → 使用 find()                                           │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 一、filter() —— 筛选所有符合条件的元素

### 1.1 核心概念

> **filter()** 方法创建一个**新数组**，包含通过所提供函数实现的测试的**所有元素**。

### 📊 工作原理可视化

```
┌─────────────────────────────────────────────────────────────────┐
│                    filter() 工作流程                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   原数组: [1, 2, 3, 4, 5, 6]                                     │
│                                                                 │
│   条件: element >= 3                                            │
│                                                                 │
│   ┌───┬───┬───┬───┬───┬───┐                                    │
│   │ 1 │ 2 │ 3 │ 4 │ 5 │ 6 │                                    │
│   └─┬─┴─┬─┴─┬─┴─┬─┴─┬─┴─┬─┘                                    │
│     │   │   │   │   │   │                                      │
│     ▼   ▼   ▼   ▼   ▼   ▼                                      │
│     ✗   ✗   ✓   ✓   ✓   ✓    ← 逐个检查                        │
│             │   │   │   │                                      │
│             ▼   ▼   ▼   ▼                                      │
│           ┌───┬───┬───┬───┐                                    │
│           │ 3 │ 4 │ 5 │ 6 │  ← 新数组（原数组不变）              │
│           └───┴───┴───┴───┘                                    │
│                                                                 │
│   特点：遍历整个数组，收集所有符合条件的元素                       │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 基础语法

```javascript
array.filter(callback(element[, index[, array]])[, thisArg])
```

| 参数 | 说明 |
|------|------|
| `callback` | 测试函数，返回 true 表示保留该元素 |
| `element` | 当前正在处理的元素 |
| `index` | 当前元素的索引（可选） |
| `array` | 调用 filter 的数组本身（可选） |
| `thisArg` | 执行 callback 时的 this 值（可选） |

### 1.3 代码示例

```javascript
// ===== 基础用法：找出等于 2 的元素 =====
function isBigEnough(element) {
    return element == 2;
}

const filtered = [1, 2, 3, 4, 5, 6].filter(isBigEnough);
console.log(filtered);  // [2]

// ===== 箭头函数简写 =====
const nums = [1, 2, 3, 4, 5, 6];

// 找出所有大于 3 的数
const result = nums.filter(x => x > 3);
console.log(result);  // [4, 5, 6]

// 找出所有偶数
const evens = nums.filter(x => x % 2 === 0);
console.log(evens);  // [2, 4, 6]

// ===== 过滤对象数组 =====
const users = [
    { name: 'Alice', age: 25 },
    { name: 'Bob', age: 17 },
    { name: 'Charlie', age: 30 }
];

// 找出所有成年人
const adults = users.filter(user => user.age >= 18);
console.log(adults);
// [{ name: 'Alice', age: 25 }, { name: 'Charlie', age: 30 }]

// ===== 使用 index 参数 =====
const arr = ['a', 'b', 'c', 'd', 'e'];

// 找出索引为偶数的元素
const evenIndex = arr.filter((item, index) => index % 2 === 0);
console.log(evenIndex);  // ['a', 'c', 'e']
```

### 1.4 filter 的特点

```
┌─────────────────────────────────────────────────────────────────┐
│                    filter() 特点总结                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ✓ 返回值：新数组（包含所有符合条件的元素）                       │
│   ✓ 原数组：不会被修改                                           │
│   ✓ 空结果：返回空数组 []，不是 undefined                         │
│   ✓ 遍历：会遍历整个数组（即使已找到符合条件的元素）                │
│   ✓ 回调返回：true 保留，false 过滤掉                             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 二、find() —— 查找第一个符合条件的元素

### 2.1 核心概念

> **find()** 方法返回数组中满足提供的测试函数的**第一个元素的值**。否则返回 **undefined**。

### 📊 工作原理可视化

```
┌─────────────────────────────────────────────────────────────────┐
│                    find() 工作流程                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   原数组: [1, 2, 3, 4, 5, 6]                                     │
│                                                                 │
│   条件: element >= 3                                            │
│                                                                 │
│   ┌───┬───┬───┬───┬───┬───┐                                    │
│   │ 1 │ 2 │ 3 │ 4 │ 5 │ 6 │                                    │
│   └─┬─┴─┬─┴─┬─┴───┴───┴───┘                                    │
│     │   │   │                                                  │
│     ▼   ▼   ▼                                                  │
│     ✗   ✗   ✓ ← 找到了！立即停止                                │
│             │                                                  │
│             ▼                                                  │
│           返回 3（单个值）                                       │
│                                                                 │
│   特点：找到第一个就停止，不继续遍历                               │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 基础语法

```javascript
array.find(callback(element[, index[, array]])[, thisArg])
```

### 2.3 代码示例

```javascript
// ===== 基础用法 =====
const arr = [1, 2, 3, 4, 5, 6];

function finds(x) {
    return x === 2;  // 注意：应该用 === 而不是 ===
}

console.log(arr.find(finds));  // 2

// ===== 箭头函数简写 =====
const nums = [1, 2, 3, 4, 5, 6];

// 找出第一个大于 3 的数
const result = nums.find(x => x > 3);
console.log(result);  // 4（只返回第一个）

// 找不到时返回 undefined
const notFound = nums.find(x => x > 10);
console.log(notFound);  // undefined

// ===== 查找对象数组 =====
const users = [
    { id: 1, name: 'Alice' },
    { id: 2, name: 'Bob' },
    { id: 3, name: 'Charlie' }
];

// 根据 id 查找用户
const user = users.find(u => u.id === 2);
console.log(user);  // { id: 2, name: 'Bob' }

// ===== 重要：find 返回的是引用 =====
const found = users.find(u => u.id === 1);
found.name = 'Modified';
console.log(users[0].name);  // 'Modified' - 原数组也被修改了！
```

### 2.4 find 的特点

```
┌─────────────────────────────────────────────────────────────────┐
│                    find() 特点总结                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ✓ 返回值：第一个符合条件的元素值（不是数组）                      │
│   ✓ 找不到：返回 undefined                                       │
│   ✓ 性能：找到后立即停止遍历                                      │
│   ✓ 引用类型：返回的是引用，修改会影响原数组                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 三、完整的数组查找方法对比

### 📊 方法大全

```
┌────────────┬─────────────────────────┬──────────────────┬─────────────────┐
│   方法名    │          参数           │       描述        │      返回值      │
├────────────┼─────────────────────────┼──────────────────┼─────────────────┤
│  includes  │ searchElement, fromIndex│ 是否包含指定的值   │     布尔值       │
├────────────┼─────────────────────────┼──────────────────┼─────────────────┤
│  indexOf   │ searchElement, fromIndex│ 首次出现的索引     │  索引值，或 -1   │
├────────────┼─────────────────────────┼──────────────────┼─────────────────┤
│lastIndexOf │ searchElement, fromIndex│ 最后出现的索引     │  索引值，或 -1   │
├────────────┼─────────────────────────┼──────────────────┼─────────────────┤
│    some    │   callback[, thisArg]   │ 是否有符合条件的   │     布尔值       │
├────────────┼─────────────────────────┼──────────────────┼─────────────────┤
│   every    │   callback[, thisArg]   │ 是否全部符合条件   │     布尔值       │
├────────────┼─────────────────────────┼──────────────────┼─────────────────┤
│   filter   │   callback[, thisArg]   │ 所有符合条件的元素 │      数组        │
├────────────┼─────────────────────────┼──────────────────┼─────────────────┤
│    find    │   callback[, thisArg]   │ 第一个符合条件的   │ 元素或 undefined │
├────────────┼─────────────────────────┼──────────────────┼─────────────────┤
│ findIndex  │   callback[, thisArg]   │ 第一个符合条件的索引│  索引值，或 -1   │
└────────────┴─────────────────────────┴──────────────────┴─────────────────┘
```

### 📊 可视化记忆图

```
┌─────────────────────────────────────────────────────────────────┐
│                    数组查找方法分类                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   按返回值分类：                                                 │
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │  返回布尔值 (是/否)                                      │   │
│   │  ├── includes()  → 包含这个值吗？                        │   │
│   │  ├── some()      → 有符合条件的吗？（有一个就行）         │   │
│   │  └── every()     → 全部符合条件吗？（必须全部）           │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │  返回索引 (位置)                                         │   │
│   │  ├── indexOf()     → 这个值在哪？（从前往后）            │   │
│   │  ├── lastIndexOf() → 这个值在哪？（从后往前）            │   │
│   │  └── findIndex()   → 第一个符合条件的在哪？              │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │  返回元素 (值)                                           │   │
│   │  ├── find()   → 第一个符合条件的元素                     │   │
│   │  └── filter() → 所有符合条件的元素（数组）               │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 四、经典面试代码

### 4.1 从数组 [1,2,3,4,5,6] 中找出值为 2 的元素

```javascript
const arr = [1, 2, 3, 4, 5, 6];

// 方法一：filter() - 返回数组
const result1 = arr.filter(x => x === 2);
console.log(result1);  // [2]

// 方法二：find() - 返回元素
const result2 = arr.find(x => x === 2);
console.log(result2);  // 2

// 方法三：indexOf() - 返回索引
const index = arr.indexOf(2);
console.log(index);  // 1
console.log(arr[index]);  // 2

// 方法四：includes() - 返回布尔值
const exists = arr.includes(2);
console.log(exists);  // true
```

### 4.2 各方法使用场景

```javascript
const products = [
    { id: 1, name: '手机', price: 5000, inStock: true },
    { id: 2, name: '电脑', price: 8000, inStock: false },
    { id: 3, name: '平板', price: 3000, inStock: true },
    { id: 4, name: '耳机', price: 500, inStock: true }
];

// ===== includes：简单值判断 =====
const ids = [1, 2, 3];
console.log(ids.includes(2));  // true

// ===== indexOf：获取位置 =====
const names = ['手机', '电脑', '平板'];
console.log(names.indexOf('电脑'));  // 1

// ===== some：是否存在（有一个就行）=====
const hasExpensive = products.some(p => p.price > 7000);
console.log(hasExpensive);  // true（电脑 8000）

// ===== every：是否全部满足 =====
const allInStock = products.every(p => p.inStock);
console.log(allInStock);  // false（电脑缺货）

// ===== filter：筛选所有符合条件的 =====
const inStockProducts = products.filter(p => p.inStock);
console.log(inStockProducts.length);  // 3

// ===== find：找第一个符合条件的 =====
const firstExpensive = products.find(p => p.price > 4000);
console.log(firstExpensive.name);  // '手机'

// ===== findIndex：找第一个符合条件的索引 =====
const expensiveIndex = products.findIndex(p => p.price > 4000);
console.log(expensiveIndex);  // 0
```

### 4.3 手写 filter 和 find

```javascript
// ===== 手写 filter =====
Array.prototype.myFilter = function(callback, thisArg) {
    if (typeof callback !== 'function') {
        throw new TypeError(callback + ' is not a function');
    }
    
    const result = [];
    for (let i = 0; i < this.length; i++) {
        // 跳过空位
        if (!(i in this)) continue;
        
        // 如果回调返回 true，则添加到结果数组
        if (callback.call(thisArg, this[i], i, this)) {
            result.push(this[i]);
        }
    }
    return result;
};

// 测试
console.log([1, 2, 3, 4, 5].myFilter(x => x > 2));  // [3, 4, 5]

// ===== 手写 find =====
Array.prototype.myFind = function(callback, thisArg) {
    if (typeof callback !== 'function') {
        throw new TypeError(callback + ' is not a function');
    }
    
    for (let i = 0; i < this.length; i++) {
        // 跳过空位
        if (!(i in this)) continue;
        
        // 找到第一个就返回
        if (callback.call(thisArg, this[i], i, this)) {
            return this[i];
        }
    }
    // 找不到返回 undefined
    return undefined;
};

// 测试
console.log([1, 2, 3, 4, 5].myFind(x => x > 2));  // 3

// ===== 手写 findIndex =====
Array.prototype.myFindIndex = function(callback, thisArg) {
    if (typeof callback !== 'function') {
        throw new TypeError(callback + ' is not a function');
    }
    
    for (let i = 0; i < this.length; i++) {
        if (!(i in this)) continue;
        
        if (callback.call(thisArg, this[i], i, this)) {
            return i;  // 返回索引
        }
    }
    return -1;  // 找不到返回 -1
};

// 测试
console.log([1, 2, 3, 4, 5].myFindIndex(x => x > 2));  // 2
```

---

## 五、filter vs find 核心对比

### 📊 对比表

```
┌────────────────┬─────────────────────────┬─────────────────────────┐
│     特性        │       filter()          │         find()          │
├────────────────┼─────────────────────────┼─────────────────────────┤
│    返回值       │   数组（可能为空 []）    │   元素或 undefined      │
├────────────────┼─────────────────────────┼─────────────────────────┤
│    返回数量     │   所有符合条件的         │   只返回第一个          │
├────────────────┼─────────────────────────┼─────────────────────────┤
│    遍历行为     │   遍历整个数组           │   找到后立即停止        │
├────────────────┼─────────────────────────┼─────────────────────────┤
│    性能        │   较慢（全量遍历）        │   较快（提前终止）       │
├────────────────┼─────────────────────────┼─────────────────────────┤
│    适用场景     │   需要所有匹配项         │   只需要一个匹配项       │
├────────────────┼─────────────────────────┼─────────────────────────┤
│    比喻        │   筛子，捞一网           │   探针，钓一条           │
└────────────────┴─────────────────────────┴─────────────────────────┘
```

### 📊 形象比喻

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   filter() - 筛子 🥅                                            │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │                                                         │   │
│   │   原数组: 🐟🐠🐡🐟🐠🐡🐟                                  │   │
│   │                  │                                      │   │
│   │                  ▼                                      │   │
│   │            ┌─────────┐                                  │   │
│   │            │  筛子   │  ← 条件：红色的鱼                 │   │
│   │            └────┬────┘                                  │   │
│   │                 │                                       │   │
│   │                 ▼                                       │   │
│   │   结果: [🐟, 🐟, 🐟]  ← 所有红色的鱼都被捞出来            │   │
│   │                                                         │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│   find() - 钓竿 🎣                                              │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │                                                         │   │
│   │   原数组: 🐟🐠🐡🐟🐠🐡🐟                                  │   │
│   │            │                                            │   │
│   │            ▼                                            │   │
│   │          🎣 ← 条件：红色的鱼                             │   │
│   │            │                                            │   │
│   │            ▼                                            │   │
│   │   结果: 🐟  ← 钓到第一条就收工                            │   │
│   │                                                         │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 六、完整知识图谱

```
                         数组查找方法
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
    返回布尔值              返回索引              返回元素
        │                     │                     │
   ┌────┼────┐          ┌────┼────┐          ┌────┼────┐
   │    │    │          │    │    │          │         │
includes some every  indexOf lastIndexOf  find    filter
   │    │    │          │    findIndex      │         │
   │    │    │          │         │         │         │
  有?  有?  全?       第一个    最后一个   第一个    所有
       │    │        出现位置   出现位置   元素值    元素值
       │    │          │         │         │         │
      有一个 必须全部    └────┬────┘         └────┬────┘
      就true 都true          │                   │
                         返回 -1              返回
                        表示没找到        undefined/[]
```

---

## 七、面试速查清单

### ✅ 必记要点

| 方法 | 返回值 | 找不到时 | 是否遍历全部 |
|------|--------|----------|-------------|
| `filter` | 数组 | `[]` | 是 |
| `find` | 元素 | `undefined` | 否（找到就停） |
| `findIndex` | 索引 | `-1` | 否（找到就停） |
| `indexOf` | 索引 | `-1` | 否（找到就停） |
| `includes` | 布尔 | `false` | 否（找到就停） |
| `some` | 布尔 | `false` | 否（找到就停） |
| `every` | 布尔 | `true`（空数组） | 否（找到false就停） |

### 🎯 面试高频问题

1. **filter 和 find 的区别？**
   → filter 返回所有符合条件的（数组），find 返回第一个（元素）

2. **find 找不到返回什么？**
   → undefined（不是 null，不是 -1）

3. **filter 找不到返回什么？**
   → 空数组 []（不是 undefined）

4. **哪个性能更好？**
   → find 更好（找到就停止），filter 会遍历全部

5. **如何判断数组是否包含某元素？**
   → includes()（返回布尔值）或 indexOf() !== -1

---

## 🔑 核心奥义

> **一句话记住区别：**
>
> | | filter | find |
> |---|---|---|
> | **比喻** | 🥅 筛子捞一网 | 🎣 钓竿钓一条 |
> | **返回** | 数组（所有符合的） | 单值（第一个符合的） |
> | **找不到** | `[]` 空数组 | `undefined` |
>
> **filter 是批发，find 是零售！**