// 严格模式下: this 指向 undefined
function f2(){
    "use strict";
    return this;
}

f2() === undefined; // true

// call 方法: this 指向传入的指定对象 o
function add(c, d) {
    return this.a + this.b + c + d;
}
var o = {a: 1, b: 3};
add.call(o, 5, 7); // 16



# 显式绑定 - 第一性原理拆解

## 🎯 本质：就三个方法

```
┌─────────────────────────────────────────────┐
│           显式绑定 = 手动指定 this           │
│                                             │
│    call()    apply()    bind()              │
│      │          │          │                │
│      └──────────┴──────────┘                │
│           都是函数自带的方法                  │
└─────────────────────────────────────────────┘
```

---

## 📊 三者对比

| 方法 | 语法 | 是否立即执行 | 参数形式 |
|------|------|-------------|---------|
| `call` | `fn.call(obj, 参数1, 参数2)` | ✅ 立即执行 | 一个个传 |
| `apply` | `fn.apply(obj, [参数1, 参数2])` | ✅ 立即执行 | 数组传 |
| `bind` | `fn.bind(obj)` | ❌ 返回新函数 | 一个个传 |

---

## 🔍 逐个拆解

### 1️⃣ call - 立即执行，参数一个个传

```javascript
function say(age, city) {
  console.log(this.name + ', ' + age + '岁, 来自' + city);
}

var person = { name: '张三' };

// 普通调用：this 是 window
say(18, '北京');  // "undefined, 18岁, 来自北京"

// call 调用：this 是 person
say.call(person, 18, '北京');  // "张三, 18岁, 来自北京"
```

```
say.call(person, 18, '北京')
        │       │    │
        │       │    └── 第2个参数：city
        │       └─────── 第1个参数：age
        └─────────────── this 指向
```

---

### 2️⃣ apply - 立即执行，参数用数组传

```javascript
function say(age, city) {
  console.log(this.name + ', ' + age + '岁, 来自' + city);
}

var person = { name: '张三' };

// apply 调用：参数放数组里
say.apply(person, [18, '北京']);  // "张三, 18岁, 来自北京"
```

```
say.apply(person, [18, '北京'])
         │        │
         │        └── 参数数组
         └─────────── this 指向
```

### call vs apply 区别

```javascript
// 完全等价，只是传参方式不同
say.call(person, 18, '北京');     // 参数一个个传
say.apply(person, [18, '北京']);  // 参数放数组
```

**记忆口诀：**
- **a**pply → **a**rray（数组）
- **c**all → **c**omma（逗号，一个个传）

---

### 3️⃣ bind - 不执行，返回新函数

```javascript
function say(age, city) {
  console.log(this.name + ', ' + age + '岁, 来自' + city);
}

var person = { name: '张三' };

// bind 不会立即执行！
var newFn = say.bind(person);  // 返回一个新函数

// 需要手动调用
newFn(18, '北京');  // "张三, 18岁, 来自北京"
```

```
┌────────────────────────────────────────┐
│ var newFn = say.bind(person)           │
│                                        │
│ 相当于创建了一个新函数：                 │
│ newFn = function(age, city) {          │
│   // this 永远是 person                │
│   console.log(this.name + ...);        │
│ }                                      │
└────────────────────────────────────────┘
```

---

## 🎯 什么时候用哪个？

```
┌─────────────────────────────────────────────────┐
│                                                 │
│  需要立即执行？                                  │
│       │                                         │
│       ├── 是 ──→ 参数是数组？                    │
│       │              │                          │
│       │              ├── 是 ──→ apply           │
│       │              └── 否 ──→ call            │
│       │                                         │
│       └── 否 ──→ bind（先绑定，以后再调用）       │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

## 📝 实际应用场景

### 场景1：借用方法

```javascript
var obj1 = {
  name: '张三',
  say: function() {
    console.log(this.name);
  }
};

var obj2 = { name: '李四' };

// obj2 没有 say 方法，但可以借用 obj1 的
obj1.say.call(obj2);  // "李四"
```

### 场景2：apply 处理数组

```javascript
var numbers = [5, 2, 9, 1, 7];

// Math.max 不接受数组，但 apply 可以展开
Math.max.apply(null, numbers);  // 9

// 等价于
Math.max(5, 2, 9, 1, 7);  // 9
```

### 场景3：bind 绑定事件

```javascript
var obj = {
  name: '按钮',
  handleClick: function() {
    console.log(this.name + '被点击');
  }
};

// ❌ 直接传：this 会丢失
button.onclick = obj.handleClick;  // this 变成 button

// ✅ 用 bind：this 永远是 obj
button.onclick = obj.handleClick.bind(obj);  // "按钮被点击"
```

---

## 🏆 总结一张图

```
           显式绑定（手动指定 this）
                    │
     ┌──────────────┼──────────────┐
     │              │              │
   call          apply          bind
     │              │              │
  立即执行       立即执行       返回新函数
  参数逗号隔开   参数用数组     以后再调用
     │              │              │
     └──────────────┴──────────────┘
                    │
            都是：fn.xxx(this指向, 参数)
```

---

还有疑问吗？



电商运营方程 基于商业价值公式
![alt text](images/1770177645224.png)
![alt text](images/1770177688634.png)